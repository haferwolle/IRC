NULL = 

AM_CPPFLAGS = \
	$(IRCCLIENT_CFLAGS) \
	-include $(CONFIG_HEADER) \
	$(NULL)

bin_PROGRAMS = IRCClient

IRCClient_SOURCES = \
	main.vala \
	$(NULL)

IRCClient_LDADD = \
	$(IRCCLIENT_LIBS) \
	$(NULL)

EXTRA_DIST = \
	$(NULL)

DISTCLEANFILES = \
	$(NULL)

